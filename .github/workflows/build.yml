name: build_action

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: make
      run: |
        sudo apt-get install gcc make unzip libxrandr-dev libxinerama-dev libxcursor-dev libxi-dev mono-runtime binutils-mingw-w64-x86-64
        make all
