The `*.a` in this folder are for windows builds only because I was lazy to find out how to build them automatically.  
Linux build compiles the code from submodules on build. (`umka/build/libumka.a` and `glfw/src/libglfw3.a`)
