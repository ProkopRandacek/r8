This folder contains the umka scripts.  
`r8/*` are R8 library files. `game.um` is a scripts that is executed by the binary on run (specifically it executes `./scripts/game.um`). It contains some example usage of the API by default. If you want to create something in this engine this is the file where you write your code.
