/*#include "raycast.h"

const int STEPSNUM = 1024;
const float COLLISION_THRESHOLD = 0.00001f;
const float MAX_TRACE_DIST = 100.0f;

extern const int groupNum;
extern const int shapeNum;


void d2Group(vec3 pos, int i) {
	float aDist, bDist;
}

rayHit rayMarch(vec3 rayOrigin, vec3 rayDir) {
	float dTravel = 0.0f;
	vec3 currentPos;
	float safeDist;

	for (int i = 0; i < STEPSNUM; i++) {
		currentPos = vAdd(rayOrigin, vMultf(rayDir, dTravel));

	}
}*/
